// Generated by dedot cli

import type {
  GenericChainTx,
  GenericTxCall,
  GenericChainKnownTypes,
  ISubmittableExtrinsic,
  ISubmittableResult,
  IRuntimeTxCall,
} from "dedot/types";
import type {
  MultiAddressLike,
  Extrinsic,
  BytesLike,
  H256,
  AccountId32Like,
  FixedBytes,
} from "dedot/codecs";
import type {
  ParachainTemplateRuntimeRuntimeCallLike,
  SpRuntimeMultiSignature,
  FrameSystemEventRecord,
  CumulusPalletParachainSystemParachainInherentBasicParachainInherentData,
  CumulusPalletParachainSystemParachainInherentInboundMessagesData,
  PalletBalancesAdjustmentDirection,
  SpWeightsWeightV2Weight,
  ParachainTemplateRuntimeOriginCaller,
  ParachainTemplateRuntimeSessionKeys,
  XcmVersionedLocation,
  XcmVersionedXcm,
  XcmVersionedAssets,
  StagingXcmV5Location,
  XcmV3WeightLimit,
  StagingXcmExecutorAssetTransferTransferType,
  XcmVersionedAssetId,
  CumulusPrimitivesCoreAggregateMessageOrigin,
  TcPrimitivesKeywordKind,
  TcPrimitivesAlignment,
  TcPrimitivesBattlekitType,
  TcPrimitivesWeaponRange,
  TcPrimitivesStatProfile,
  PalletBattleBattleReport,
  TcPrimitivesExplorationTable,
} from "./types.js";

export type ChainSubmittableExtrinsic<
  T extends IRuntimeTxCall = ParachainTemplateRuntimeRuntimeCallLike,
  ChainKnownTypes extends GenericChainKnownTypes = GenericChainKnownTypes,
> = Extrinsic<
  MultiAddressLike,
  T,
  SpRuntimeMultiSignature,
  ChainKnownTypes["Extra"]
> &
  ISubmittableExtrinsic<
    ISubmittableResult<FrameSystemEventRecord>,
    ChainKnownTypes["AssetId"]
  >;

export type TxCall<
  ChainKnownTypes extends GenericChainKnownTypes = GenericChainKnownTypes,
> = (
  ...args: any[]
) => ChainSubmittableExtrinsic<
  ParachainTemplateRuntimeRuntimeCallLike,
  ChainKnownTypes
>;

export interface ChainTx<
  ChainKnownTypes extends GenericChainKnownTypes = GenericChainKnownTypes,
> extends GenericChainTx<TxCall<ChainKnownTypes>> {
  /**
   * Pallet `System`'s transaction calls
   **/
  system: {
    /**
     * Make some on-chain remark.
     *
     * Can be executed by every `origin`.
     *
     * @param {BytesLike} remark
     **/
    remark: GenericTxCall<
      (remark: BytesLike) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "Remark";
            params: { remark: BytesLike };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Set the number of pages in the WebAssembly environment's heap.
     *
     * @param {bigint} pages
     **/
    setHeapPages: GenericTxCall<
      (pages: bigint) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "SetHeapPages";
            params: { pages: bigint };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Set the new runtime code.
     *
     * @param {BytesLike} code
     **/
    setCode: GenericTxCall<
      (code: BytesLike) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "SetCode";
            params: { code: BytesLike };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Set the new runtime code without doing any checks of the given `code`.
     *
     * Note that runtime upgrades will not run if this is called with a not-increasing spec
     * version!
     *
     * @param {BytesLike} code
     **/
    setCodeWithoutChecks: GenericTxCall<
      (code: BytesLike) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "SetCodeWithoutChecks";
            params: { code: BytesLike };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Set some items of storage.
     *
     * @param {Array<[BytesLike, BytesLike]>} items
     **/
    setStorage: GenericTxCall<
      (items: Array<[BytesLike, BytesLike]>) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "SetStorage";
            params: { items: Array<[BytesLike, BytesLike]> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Kill some items from storage.
     *
     * @param {Array<BytesLike>} keys
     **/
    killStorage: GenericTxCall<
      (keys: Array<BytesLike>) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "KillStorage";
            params: { keys: Array<BytesLike> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Kill all storage items with a key that starts with the given prefix.
     *
     * **NOTE:** We rely on the Root origin to provide us the number of subkeys under
     * the prefix we are removing to accurately calculate the weight of this function.
     *
     * @param {BytesLike} prefix
     * @param {number} subkeys
     **/
    killPrefix: GenericTxCall<
      (
        prefix: BytesLike,
        subkeys: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "KillPrefix";
            params: { prefix: BytesLike; subkeys: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Make some on-chain remark and emit event.
     *
     * @param {BytesLike} remark
     **/
    remarkWithEvent: GenericTxCall<
      (remark: BytesLike) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "RemarkWithEvent";
            params: { remark: BytesLike };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Authorize an upgrade to a given `code_hash` for the runtime. The runtime can be supplied
     * later.
     *
     * This call requires Root origin.
     *
     * @param {H256} codeHash
     **/
    authorizeUpgrade: GenericTxCall<
      (codeHash: H256) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "AuthorizeUpgrade";
            params: { codeHash: H256 };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Authorize an upgrade to a given `code_hash` for the runtime. The runtime can be supplied
     * later.
     *
     * WARNING: This authorizes an upgrade that will take place without any safety checks, for
     * example that the spec name remains the same and that the version number increases. Not
     * recommended for normal use. Use `authorize_upgrade` instead.
     *
     * This call requires Root origin.
     *
     * @param {H256} codeHash
     **/
    authorizeUpgradeWithoutChecks: GenericTxCall<
      (codeHash: H256) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "AuthorizeUpgradeWithoutChecks";
            params: { codeHash: H256 };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Provide the preimage (runtime binary) `code` for an upgrade that has been authorized.
     *
     * If the authorization required a version check, this call will ensure the spec name
     * remains unchanged and that the spec version has increased.
     *
     * Depending on the runtime's `OnSetCode` configuration, this function may directly apply
     * the new `code` in the same block or attempt to schedule the upgrade.
     *
     * All origins are allowed.
     *
     * @param {BytesLike} code
     **/
    applyAuthorizedUpgrade: GenericTxCall<
      (code: BytesLike) => ChainSubmittableExtrinsic<
        {
          pallet: "System";
          palletCall: {
            name: "ApplyAuthorizedUpgrade";
            params: { code: BytesLike };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `ParachainSystem`'s transaction calls
   **/
  parachainSystem: {
    /**
     * Set the current validation data.
     *
     * This should be invoked exactly once per block. It will panic at the finalization
     * phase if the call was not invoked.
     *
     * The dispatch origin for this call must be `Inherent`
     *
     * As a side effect, this function upgrades the current validation function
     * if the appropriate time has come.
     *
     * @param {CumulusPalletParachainSystemParachainInherentBasicParachainInherentData} data
     * @param {CumulusPalletParachainSystemParachainInherentInboundMessagesData} inboundMessagesData
     **/
    setValidationData: GenericTxCall<
      (
        data: CumulusPalletParachainSystemParachainInherentBasicParachainInherentData,
        inboundMessagesData: CumulusPalletParachainSystemParachainInherentInboundMessagesData,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "ParachainSystem";
          palletCall: {
            name: "SetValidationData";
            params: {
              data: CumulusPalletParachainSystemParachainInherentBasicParachainInherentData;
              inboundMessagesData: CumulusPalletParachainSystemParachainInherentInboundMessagesData;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {BytesLike} message
     **/
    sudoSendUpwardMessage: GenericTxCall<
      (message: BytesLike) => ChainSubmittableExtrinsic<
        {
          pallet: "ParachainSystem";
          palletCall: {
            name: "SudoSendUpwardMessage";
            params: { message: BytesLike };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Timestamp`'s transaction calls
   **/
  timestamp: {
    /**
     * Set the current time.
     *
     * This call should be invoked exactly once per block. It will panic at the finalization
     * phase, if this call hasn't been invoked by that time.
     *
     * The timestamp should be greater than the previous one by the amount specified by
     * [`Config::MinimumPeriod`].
     *
     * The dispatch origin for this call must be _None_.
     *
     * This dispatch class is _Mandatory_ to ensure it gets executed in the block. Be aware
     * that changing the complexity of this call could result exhausting the resources in a
     * block to execute any other calls.
     *
     * ## Complexity
     * - `O(1)` (Note that implementations of `OnTimestampSet` must also be `O(1)`)
     * - 1 storage read and 1 storage mutation (codec `O(1)` because of `DidUpdate::take` in
     * `on_finalize`)
     * - 1 event handler `on_timestamp_set`. Must be `O(1)`.
     *
     * @param {bigint} now
     **/
    set: GenericTxCall<
      (now: bigint) => ChainSubmittableExtrinsic<
        {
          pallet: "Timestamp";
          palletCall: {
            name: "Set";
            params: { now: bigint };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `ParachainInfo`'s transaction calls
   **/
  parachainInfo: {
    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Balances`'s transaction calls
   **/
  balances: {
    /**
     * Transfer some liquid free balance to another account.
     *
     * `transfer_allow_death` will set the `FreeBalance` of the sender and receiver.
     * If the sender's account is below the existential deposit as a result
     * of the transfer, the account will be reaped.
     *
     * The dispatch origin for this call must be `Signed` by the transactor.
     *
     * @param {MultiAddressLike} dest
     * @param {bigint} value
     **/
    transferAllowDeath: GenericTxCall<
      (
        dest: MultiAddressLike,
        value: bigint,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Balances";
          palletCall: {
            name: "TransferAllowDeath";
            params: { dest: MultiAddressLike; value: bigint };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Exactly as `transfer_allow_death`, except the origin must be root and the source account
     * may be specified.
     *
     * @param {MultiAddressLike} source
     * @param {MultiAddressLike} dest
     * @param {bigint} value
     **/
    forceTransfer: GenericTxCall<
      (
        source: MultiAddressLike,
        dest: MultiAddressLike,
        value: bigint,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Balances";
          palletCall: {
            name: "ForceTransfer";
            params: {
              source: MultiAddressLike;
              dest: MultiAddressLike;
              value: bigint;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Same as the [`transfer_allow_death`] call, but with a check that the transfer will not
     * kill the origin account.
     *
     * 99% of the time you want [`transfer_allow_death`] instead.
     *
     * [`transfer_allow_death`]: struct.Pallet.html#method.transfer
     *
     * @param {MultiAddressLike} dest
     * @param {bigint} value
     **/
    transferKeepAlive: GenericTxCall<
      (
        dest: MultiAddressLike,
        value: bigint,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Balances";
          palletCall: {
            name: "TransferKeepAlive";
            params: { dest: MultiAddressLike; value: bigint };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Transfer the entire transferable balance from the caller account.
     *
     * NOTE: This function only attempts to transfer _transferable_ balances. This means that
     * any locked, reserved, or existential deposits (when `keep_alive` is `true`), will not be
     * transferred by this function. To ensure that this function results in a killed account,
     * you might need to prepare the account by removing any reference counters, storage
     * deposits, etc...
     *
     * The dispatch origin of this call must be Signed.
     *
     * - `dest`: The recipient of the transfer.
     * - `keep_alive`: A boolean to determine if the `transfer_all` operation should send all
     * of the funds the account has, causing the sender account to be killed (false), or
     * transfer everything except at least the existential deposit, which will guarantee to
     * keep the sender account alive (true).
     *
     * @param {MultiAddressLike} dest
     * @param {boolean} keepAlive
     **/
    transferAll: GenericTxCall<
      (
        dest: MultiAddressLike,
        keepAlive: boolean,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Balances";
          palletCall: {
            name: "TransferAll";
            params: { dest: MultiAddressLike; keepAlive: boolean };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Unreserve some balance from a user by force.
     *
     * Can only be called by ROOT.
     *
     * @param {MultiAddressLike} who
     * @param {bigint} amount
     **/
    forceUnreserve: GenericTxCall<
      (
        who: MultiAddressLike,
        amount: bigint,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Balances";
          palletCall: {
            name: "ForceUnreserve";
            params: { who: MultiAddressLike; amount: bigint };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Upgrade a specified account.
     *
     * - `origin`: Must be `Signed`.
     * - `who`: The account to be upgraded.
     *
     * This will waive the transaction fee if at least all but 10% of the accounts needed to
     * be upgraded. (We let some not have to be upgraded just in order to allow for the
     * possibility of churn).
     *
     * @param {Array<AccountId32Like>} who
     **/
    upgradeAccounts: GenericTxCall<
      (who: Array<AccountId32Like>) => ChainSubmittableExtrinsic<
        {
          pallet: "Balances";
          palletCall: {
            name: "UpgradeAccounts";
            params: { who: Array<AccountId32Like> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Set the regular balance of a given account.
     *
     * The dispatch origin for this call is `root`.
     *
     * @param {MultiAddressLike} who
     * @param {bigint} newFree
     **/
    forceSetBalance: GenericTxCall<
      (
        who: MultiAddressLike,
        newFree: bigint,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Balances";
          palletCall: {
            name: "ForceSetBalance";
            params: { who: MultiAddressLike; newFree: bigint };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Adjust the total issuance in a saturating way.
     *
     * Can only be called by root and always needs a positive `delta`.
     *
     * # Example
     *
     * @param {PalletBalancesAdjustmentDirection} direction
     * @param {bigint} delta
     **/
    forceAdjustTotalIssuance: GenericTxCall<
      (
        direction: PalletBalancesAdjustmentDirection,
        delta: bigint,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Balances";
          palletCall: {
            name: "ForceAdjustTotalIssuance";
            params: {
              direction: PalletBalancesAdjustmentDirection;
              delta: bigint;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Burn the specified liquid free balance from the origin account.
     *
     * If the origin's account ends up below the existential deposit as a result
     * of the burn and `keep_alive` is false, the account will be reaped.
     *
     * Unlike sending funds to a _burn_ address, which merely makes the funds inaccessible,
     * this `burn` operation will reduce total issuance by the amount _burned_.
     *
     * @param {bigint} value
     * @param {boolean} keepAlive
     **/
    burn: GenericTxCall<
      (
        value: bigint,
        keepAlive: boolean,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Balances";
          palletCall: {
            name: "Burn";
            params: { value: bigint; keepAlive: boolean };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Sudo`'s transaction calls
   **/
  sudo: {
    /**
     * Authenticates the sudo key and dispatches a function call with `Root` origin.
     *
     * @param {ParachainTemplateRuntimeRuntimeCallLike} call
     **/
    sudo: GenericTxCall<
      (
        call: ParachainTemplateRuntimeRuntimeCallLike,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Sudo";
          palletCall: {
            name: "Sudo";
            params: { call: ParachainTemplateRuntimeRuntimeCallLike };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Authenticates the sudo key and dispatches a function call with `Root` origin.
     * This function does not check the weight of the call, and instead allows the
     * Sudo user to specify the weight of the call.
     *
     * The dispatch origin for this call must be _Signed_.
     *
     * @param {ParachainTemplateRuntimeRuntimeCallLike} call
     * @param {SpWeightsWeightV2Weight} weight
     **/
    sudoUncheckedWeight: GenericTxCall<
      (
        call: ParachainTemplateRuntimeRuntimeCallLike,
        weight: SpWeightsWeightV2Weight,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Sudo";
          palletCall: {
            name: "SudoUncheckedWeight";
            params: {
              call: ParachainTemplateRuntimeRuntimeCallLike;
              weight: SpWeightsWeightV2Weight;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Authenticates the current sudo key and sets the given AccountId (`new`) as the new sudo
     * key.
     *
     * @param {MultiAddressLike} new_
     **/
    setKey: GenericTxCall<
      (new_: MultiAddressLike) => ChainSubmittableExtrinsic<
        {
          pallet: "Sudo";
          palletCall: {
            name: "SetKey";
            params: { new: MultiAddressLike };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Authenticates the sudo key and dispatches a function call with `Signed` origin from
     * a given account.
     *
     * The dispatch origin for this call must be _Signed_.
     *
     * @param {MultiAddressLike} who
     * @param {ParachainTemplateRuntimeRuntimeCallLike} call
     **/
    sudoAs: GenericTxCall<
      (
        who: MultiAddressLike,
        call: ParachainTemplateRuntimeRuntimeCallLike,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Sudo";
          palletCall: {
            name: "SudoAs";
            params: {
              who: MultiAddressLike;
              call: ParachainTemplateRuntimeRuntimeCallLike;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Permanently removes the sudo key.
     *
     * **This cannot be un-done.**
     *
     **/
    removeKey: GenericTxCall<
      () => ChainSubmittableExtrinsic<
        {
          pallet: "Sudo";
          palletCall: {
            name: "RemoveKey";
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Utility`'s transaction calls
   **/
  utility: {
    /**
     * Send a batch of dispatch calls.
     *
     * May be called from any origin except `None`.
     *
     * - `calls`: The calls to be dispatched from the same origin. The number of call must not
     * exceed the constant: `batched_calls_limit` (available in constant metadata).
     *
     * If origin is root then the calls are dispatched without checking origin filter. (This
     * includes bypassing `frame_system::Config::BaseCallFilter`).
     *
     * ## Complexity
     * - O(C) where C is the number of calls to be batched.
     *
     * This will return `Ok` in all circumstances. To determine the success of the batch, an
     * event is deposited. If a call failed and the batch was interrupted, then the
     * `BatchInterrupted` event is deposited, along with the number of successful calls made
     * and the error of the failed call. If all were successful, then the `BatchCompleted`
     * event is deposited.
     *
     * @param {Array<ParachainTemplateRuntimeRuntimeCallLike>} calls
     **/
    batch: GenericTxCall<
      (
        calls: Array<ParachainTemplateRuntimeRuntimeCallLike>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Utility";
          palletCall: {
            name: "Batch";
            params: { calls: Array<ParachainTemplateRuntimeRuntimeCallLike> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Send a call through an indexed pseudonym of the sender.
     *
     * Filter from origin are passed along. The call will be dispatched with an origin which
     * use the same filter as the origin of this call.
     *
     * NOTE: If you need to ensure that any account-based filtering is not honored (i.e.
     * because you expect `proxy` to have been used prior in the call stack and you do not want
     * the call restrictions to apply to any sub-accounts), then use `as_multi_threshold_1`
     * in the Multisig pallet instead.
     *
     * NOTE: Prior to version *12, this was called `as_limited_sub`.
     *
     * The dispatch origin for this call must be _Signed_.
     *
     * @param {number} index
     * @param {ParachainTemplateRuntimeRuntimeCallLike} call
     **/
    asDerivative: GenericTxCall<
      (
        index: number,
        call: ParachainTemplateRuntimeRuntimeCallLike,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Utility";
          palletCall: {
            name: "AsDerivative";
            params: {
              index: number;
              call: ParachainTemplateRuntimeRuntimeCallLike;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Send a batch of dispatch calls and atomically execute them.
     * The whole transaction will rollback and fail if any of the calls failed.
     *
     * May be called from any origin except `None`.
     *
     * - `calls`: The calls to be dispatched from the same origin. The number of call must not
     * exceed the constant: `batched_calls_limit` (available in constant metadata).
     *
     * If origin is root then the calls are dispatched without checking origin filter. (This
     * includes bypassing `frame_system::Config::BaseCallFilter`).
     *
     * ## Complexity
     * - O(C) where C is the number of calls to be batched.
     *
     * @param {Array<ParachainTemplateRuntimeRuntimeCallLike>} calls
     **/
    batchAll: GenericTxCall<
      (
        calls: Array<ParachainTemplateRuntimeRuntimeCallLike>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Utility";
          palletCall: {
            name: "BatchAll";
            params: { calls: Array<ParachainTemplateRuntimeRuntimeCallLike> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Dispatches a function call with a provided origin.
     *
     * The dispatch origin for this call must be _Root_.
     *
     * ## Complexity
     * - O(1).
     *
     * @param {ParachainTemplateRuntimeOriginCaller} asOrigin
     * @param {ParachainTemplateRuntimeRuntimeCallLike} call
     **/
    dispatchAs: GenericTxCall<
      (
        asOrigin: ParachainTemplateRuntimeOriginCaller,
        call: ParachainTemplateRuntimeRuntimeCallLike,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Utility";
          palletCall: {
            name: "DispatchAs";
            params: {
              asOrigin: ParachainTemplateRuntimeOriginCaller;
              call: ParachainTemplateRuntimeRuntimeCallLike;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Send a batch of dispatch calls.
     * Unlike `batch`, it allows errors and won't interrupt.
     *
     * May be called from any origin except `None`.
     *
     * - `calls`: The calls to be dispatched from the same origin. The number of call must not
     * exceed the constant: `batched_calls_limit` (available in constant metadata).
     *
     * If origin is root then the calls are dispatch without checking origin filter. (This
     * includes bypassing `frame_system::Config::BaseCallFilter`).
     *
     * ## Complexity
     * - O(C) where C is the number of calls to be batched.
     *
     * @param {Array<ParachainTemplateRuntimeRuntimeCallLike>} calls
     **/
    forceBatch: GenericTxCall<
      (
        calls: Array<ParachainTemplateRuntimeRuntimeCallLike>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Utility";
          palletCall: {
            name: "ForceBatch";
            params: { calls: Array<ParachainTemplateRuntimeRuntimeCallLike> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Dispatch a function call with a specified weight.
     *
     * This function does not check the weight of the call, and instead allows the
     * Root origin to specify the weight of the call.
     *
     * The dispatch origin for this call must be _Root_.
     *
     * @param {ParachainTemplateRuntimeRuntimeCallLike} call
     * @param {SpWeightsWeightV2Weight} weight
     **/
    withWeight: GenericTxCall<
      (
        call: ParachainTemplateRuntimeRuntimeCallLike,
        weight: SpWeightsWeightV2Weight,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Utility";
          palletCall: {
            name: "WithWeight";
            params: {
              call: ParachainTemplateRuntimeRuntimeCallLike;
              weight: SpWeightsWeightV2Weight;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Dispatch a fallback call in the event the main call fails to execute.
     * May be called from any origin except `None`.
     *
     * This function first attempts to dispatch the `main` call.
     * If the `main` call fails, the `fallback` is attemted.
     * if the fallback is successfully dispatched, the weights of both calls
     * are accumulated and an event containing the main call error is deposited.
     *
     * In the event of a fallback failure the whole call fails
     * with the weights returned.
     *
     * - `main`: The main call to be dispatched. This is the primary action to execute.
     * - `fallback`: The fallback call to be dispatched in case the `main` call fails.
     *
     * ## Dispatch Logic
     * - If the origin is `root`, both the main and fallback calls are executed without
     * applying any origin filters.
     * - If the origin is not `root`, the origin filter is applied to both the `main` and
     * `fallback` calls.
     *
     * ## Use Case
     * - Some use cases might involve submitting a `batch` type call in either main, fallback
     * or both.
     *
     * @param {ParachainTemplateRuntimeRuntimeCallLike} main
     * @param {ParachainTemplateRuntimeRuntimeCallLike} fallback
     **/
    ifElse: GenericTxCall<
      (
        main: ParachainTemplateRuntimeRuntimeCallLike,
        fallback: ParachainTemplateRuntimeRuntimeCallLike,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Utility";
          palletCall: {
            name: "IfElse";
            params: {
              main: ParachainTemplateRuntimeRuntimeCallLike;
              fallback: ParachainTemplateRuntimeRuntimeCallLike;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Dispatches a function call with a provided origin.
     *
     * Almost the same as [`Pallet::dispatch_as`] but forwards any error of the inner call.
     *
     * The dispatch origin for this call must be _Root_.
     *
     * @param {ParachainTemplateRuntimeOriginCaller} asOrigin
     * @param {ParachainTemplateRuntimeRuntimeCallLike} call
     **/
    dispatchAsFallible: GenericTxCall<
      (
        asOrigin: ParachainTemplateRuntimeOriginCaller,
        call: ParachainTemplateRuntimeRuntimeCallLike,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Utility";
          palletCall: {
            name: "DispatchAsFallible";
            params: {
              asOrigin: ParachainTemplateRuntimeOriginCaller;
              call: ParachainTemplateRuntimeRuntimeCallLike;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `CollatorSelection`'s transaction calls
   **/
  collatorSelection: {
    /**
     * Set the list of invulnerable (fixed) collators. These collators must do some
     * preparation, namely to have registered session keys.
     *
     * The call will remove any accounts that have not registered keys from the set. That is,
     * it is non-atomic; the caller accepts all `AccountId`s passed in `new` _individually_ as
     * acceptable Invulnerables, and is not proposing a _set_ of new Invulnerables.
     *
     * This call does not maintain mutual exclusivity of `Invulnerables` and `Candidates`. It
     * is recommended to use a batch of `add_invulnerable` and `remove_invulnerable` instead. A
     * `batch_all` can also be used to enforce atomicity. If any candidates are included in
     * `new`, they should be removed with `remove_invulnerable_candidate` after execution.
     *
     * Must be called by the `UpdateOrigin`.
     *
     * @param {Array<AccountId32Like>} new_
     **/
    setInvulnerables: GenericTxCall<
      (new_: Array<AccountId32Like>) => ChainSubmittableExtrinsic<
        {
          pallet: "CollatorSelection";
          palletCall: {
            name: "SetInvulnerables";
            params: { new: Array<AccountId32Like> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Set the ideal number of non-invulnerable collators. If lowering this number, then the
     * number of running collators could be higher than this figure. Aside from that edge case,
     * there should be no other way to have more candidates than the desired number.
     *
     * The origin for this call must be the `UpdateOrigin`.
     *
     * @param {number} max
     **/
    setDesiredCandidates: GenericTxCall<
      (max: number) => ChainSubmittableExtrinsic<
        {
          pallet: "CollatorSelection";
          palletCall: {
            name: "SetDesiredCandidates";
            params: { max: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Set the candidacy bond amount.
     *
     * If the candidacy bond is increased by this call, all current candidates which have a
     * deposit lower than the new bond will be kicked from the list and get their deposits
     * back.
     *
     * The origin for this call must be the `UpdateOrigin`.
     *
     * @param {bigint} bond
     **/
    setCandidacyBond: GenericTxCall<
      (bond: bigint) => ChainSubmittableExtrinsic<
        {
          pallet: "CollatorSelection";
          palletCall: {
            name: "SetCandidacyBond";
            params: { bond: bigint };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Register this account as a collator candidate. The account must (a) already have
     * registered session keys and (b) be able to reserve the `CandidacyBond`.
     *
     * This call is not available to `Invulnerable` collators.
     *
     **/
    registerAsCandidate: GenericTxCall<
      () => ChainSubmittableExtrinsic<
        {
          pallet: "CollatorSelection";
          palletCall: {
            name: "RegisterAsCandidate";
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Deregister `origin` as a collator candidate. Note that the collator can only leave on
     * session change. The `CandidacyBond` will be unreserved immediately.
     *
     * This call will fail if the total number of candidates would drop below
     * `MinEligibleCollators`.
     *
     **/
    leaveIntent: GenericTxCall<
      () => ChainSubmittableExtrinsic<
        {
          pallet: "CollatorSelection";
          palletCall: {
            name: "LeaveIntent";
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Add a new account `who` to the list of `Invulnerables` collators. `who` must have
     * registered session keys. If `who` is a candidate, they will be removed.
     *
     * The origin for this call must be the `UpdateOrigin`.
     *
     * @param {AccountId32Like} who
     **/
    addInvulnerable: GenericTxCall<
      (who: AccountId32Like) => ChainSubmittableExtrinsic<
        {
          pallet: "CollatorSelection";
          palletCall: {
            name: "AddInvulnerable";
            params: { who: AccountId32Like };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Remove an account `who` from the list of `Invulnerables` collators. `Invulnerables` must
     * be sorted.
     *
     * The origin for this call must be the `UpdateOrigin`.
     *
     * @param {AccountId32Like} who
     **/
    removeInvulnerable: GenericTxCall<
      (who: AccountId32Like) => ChainSubmittableExtrinsic<
        {
          pallet: "CollatorSelection";
          palletCall: {
            name: "RemoveInvulnerable";
            params: { who: AccountId32Like };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Update the candidacy bond of collator candidate `origin` to a new amount `new_deposit`.
     *
     * Setting a `new_deposit` that is lower than the current deposit while `origin` is
     * occupying a top-`DesiredCandidates` slot is not allowed.
     *
     * This call will fail if `origin` is not a collator candidate, the updated bond is lower
     * than the minimum candidacy bond, and/or the amount cannot be reserved.
     *
     * @param {bigint} newDeposit
     **/
    updateBond: GenericTxCall<
      (newDeposit: bigint) => ChainSubmittableExtrinsic<
        {
          pallet: "CollatorSelection";
          palletCall: {
            name: "UpdateBond";
            params: { newDeposit: bigint };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * The caller `origin` replaces a candidate `target` in the collator candidate list by
     * reserving `deposit`. The amount `deposit` reserved by the caller must be greater than
     * the existing bond of the target it is trying to replace.
     *
     * This call will fail if the caller is already a collator candidate or invulnerable, the
     * caller does not have registered session keys, the target is not a collator candidate,
     * and/or the `deposit` amount cannot be reserved.
     *
     * @param {bigint} deposit
     * @param {AccountId32Like} target
     **/
    takeCandidateSlot: GenericTxCall<
      (
        deposit: bigint,
        target: AccountId32Like,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "CollatorSelection";
          palletCall: {
            name: "TakeCandidateSlot";
            params: { deposit: bigint; target: AccountId32Like };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Session`'s transaction calls
   **/
  session: {
    /**
     * Sets the session key(s) of the function caller to `keys`.
     * Allows an account to set its session key prior to becoming a validator.
     * This doesn't take effect until the next session.
     *
     * The dispatch origin of this function must be signed.
     *
     * ## Complexity
     * - `O(1)`. Actual cost depends on the number of length of `T::Keys::key_ids()` which is
     * fixed.
     *
     * @param {ParachainTemplateRuntimeSessionKeys} keys
     * @param {BytesLike} proof
     **/
    setKeys: GenericTxCall<
      (
        keys: ParachainTemplateRuntimeSessionKeys,
        proof: BytesLike,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Session";
          palletCall: {
            name: "SetKeys";
            params: {
              keys: ParachainTemplateRuntimeSessionKeys;
              proof: BytesLike;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Removes any session key(s) of the function caller.
     *
     * This doesn't take effect until the next session.
     *
     * The dispatch origin of this function must be Signed and the account must be either be
     * convertible to a validator ID using the chain's typical addressing system (this usually
     * means being a controller account) or directly convertible into a validator ID (which
     * usually means being a stash account).
     *
     * ## Complexity
     * - `O(1)` in number of key types. Actual cost depends on the number of length of
     * `T::Keys::key_ids()` which is fixed.
     *
     **/
    purgeKeys: GenericTxCall<
      () => ChainSubmittableExtrinsic<
        {
          pallet: "Session";
          palletCall: {
            name: "PurgeKeys";
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `XcmpQueue`'s transaction calls
   **/
  xcmpQueue: {
    /**
     * Suspends all XCM executions for the XCMP queue, regardless of the sender's origin.
     *
     * - `origin`: Must pass `ControllerOrigin`.
     *
     **/
    suspendXcmExecution: GenericTxCall<
      () => ChainSubmittableExtrinsic<
        {
          pallet: "XcmpQueue";
          palletCall: {
            name: "SuspendXcmExecution";
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Resumes all XCM executions for the XCMP queue.
     *
     * Note that this function doesn't change the status of the in/out bound channels.
     *
     * - `origin`: Must pass `ControllerOrigin`.
     *
     **/
    resumeXcmExecution: GenericTxCall<
      () => ChainSubmittableExtrinsic<
        {
          pallet: "XcmpQueue";
          palletCall: {
            name: "ResumeXcmExecution";
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Overwrites the number of pages which must be in the queue for the other side to be
     * told to suspend their sending.
     *
     * - `origin`: Must pass `Root`.
     * - `new`: Desired value for `QueueConfigData.suspend_value`
     *
     * @param {number} new_
     **/
    updateSuspendThreshold: GenericTxCall<
      (new_: number) => ChainSubmittableExtrinsic<
        {
          pallet: "XcmpQueue";
          palletCall: {
            name: "UpdateSuspendThreshold";
            params: { new: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Overwrites the number of pages which must be in the queue after which we drop any
     * further messages from the channel.
     *
     * - `origin`: Must pass `Root`.
     * - `new`: Desired value for `QueueConfigData.drop_threshold`
     *
     * @param {number} new_
     **/
    updateDropThreshold: GenericTxCall<
      (new_: number) => ChainSubmittableExtrinsic<
        {
          pallet: "XcmpQueue";
          palletCall: {
            name: "UpdateDropThreshold";
            params: { new: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Overwrites the number of pages which the queue must be reduced to before it signals
     * that message sending may recommence after it has been suspended.
     *
     * - `origin`: Must pass `Root`.
     * - `new`: Desired value for `QueueConfigData.resume_threshold`
     *
     * @param {number} new_
     **/
    updateResumeThreshold: GenericTxCall<
      (new_: number) => ChainSubmittableExtrinsic<
        {
          pallet: "XcmpQueue";
          palletCall: {
            name: "UpdateResumeThreshold";
            params: { new: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `PolkadotXcm`'s transaction calls
   **/
  polkadotXcm: {
    /**
     *
     * @param {XcmVersionedLocation} dest
     * @param {XcmVersionedXcm} message
     **/
    send: GenericTxCall<
      (
        dest: XcmVersionedLocation,
        message: XcmVersionedXcm,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "Send";
            params: { dest: XcmVersionedLocation; message: XcmVersionedXcm };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Teleport some assets from the local chain to some destination chain.
     *
     * **This function is deprecated: Use `limited_teleport_assets` instead.**
     *
     * Fee payment on the destination side is made from the asset in the `assets` vector of
     * index `fee_asset_item`. The weight limit for fees is not provided and thus is unlimited,
     * with all fees taken as needed from the asset.
     *
     * - `origin`: Must be capable of withdrawing the `assets` and executing XCM.
     * - `dest`: Destination context for the assets. Will typically be `[Parent,
     * Parachain(..)]` to send from parachain to parachain, or `[Parachain(..)]` to send from
     * relay to parachain.
     * - `beneficiary`: A beneficiary location for the assets in the context of `dest`. Will
     * generally be an `AccountId32` value.
     * - `assets`: The assets to be withdrawn. This should include the assets used to pay the
     * fee on the `dest` chain.
     * - `fee_asset_item`: The index into `assets` of the item which should be used to pay
     * fees.
     *
     * @param {XcmVersionedLocation} dest
     * @param {XcmVersionedLocation} beneficiary
     * @param {XcmVersionedAssets} assets
     * @param {number} feeAssetItem
     *
     * @deprecated This extrinsic uses `WeightLimit::Unlimited`, please migrate to `limited_teleport_assets` or `transfer_assets`
     **/
    teleportAssets: GenericTxCall<
      (
        dest: XcmVersionedLocation,
        beneficiary: XcmVersionedLocation,
        assets: XcmVersionedAssets,
        feeAssetItem: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "TeleportAssets";
            params: {
              dest: XcmVersionedLocation;
              beneficiary: XcmVersionedLocation;
              assets: XcmVersionedAssets;
              feeAssetItem: number;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Transfer some assets from the local chain to the destination chain through their local,
     * destination or remote reserve.
     *
     * `assets` must have same reserve location and may not be teleportable to `dest`.
     * - `assets` have local reserve: transfer assets to sovereign account of destination
     * chain and forward a notification XCM to `dest` to mint and deposit reserve-based
     * assets to `beneficiary`.
     * - `assets` have destination reserve: burn local assets and forward a notification to
     * `dest` chain to withdraw the reserve assets from this chain's sovereign account and
     * deposit them to `beneficiary`.
     * - `assets` have remote reserve: burn local assets, forward XCM to reserve chain to move
     * reserves from this chain's SA to `dest` chain's SA, and forward another XCM to `dest`
     * to mint and deposit reserve-based assets to `beneficiary`.
     *
     * **This function is deprecated: Use `limited_reserve_transfer_assets` instead.**
     *
     * Fee payment on the destination side is made from the asset in the `assets` vector of
     * index `fee_asset_item`. The weight limit for fees is not provided and thus is unlimited,
     * with all fees taken as needed from the asset.
     *
     * - `origin`: Must be capable of withdrawing the `assets` and executing XCM.
     * - `dest`: Destination context for the assets. Will typically be `[Parent,
     * Parachain(..)]` to send from parachain to parachain, or `[Parachain(..)]` to send from
     * relay to parachain.
     * - `beneficiary`: A beneficiary location for the assets in the context of `dest`. Will
     * generally be an `AccountId32` value.
     * - `assets`: The assets to be withdrawn. This should include the assets used to pay the
     * fee on the `dest` (and possibly reserve) chains.
     * - `fee_asset_item`: The index into `assets` of the item which should be used to pay
     * fees.
     *
     * @param {XcmVersionedLocation} dest
     * @param {XcmVersionedLocation} beneficiary
     * @param {XcmVersionedAssets} assets
     * @param {number} feeAssetItem
     *
     * @deprecated This extrinsic uses `WeightLimit::Unlimited`, please migrate to `limited_reserve_transfer_assets` or `transfer_assets`
     **/
    reserveTransferAssets: GenericTxCall<
      (
        dest: XcmVersionedLocation,
        beneficiary: XcmVersionedLocation,
        assets: XcmVersionedAssets,
        feeAssetItem: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "ReserveTransferAssets";
            params: {
              dest: XcmVersionedLocation;
              beneficiary: XcmVersionedLocation;
              assets: XcmVersionedAssets;
              feeAssetItem: number;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Execute an XCM message from a local, signed, origin.
     *
     * An event is deposited indicating whether `msg` could be executed completely or only
     * partially.
     *
     * No more than `max_weight` will be used in its attempted execution. If this is less than
     * the maximum amount of weight that the message could take to be executed, then no
     * execution attempt will be made.
     *
     * @param {XcmVersionedXcm} message
     * @param {SpWeightsWeightV2Weight} maxWeight
     **/
    execute: GenericTxCall<
      (
        message: XcmVersionedXcm,
        maxWeight: SpWeightsWeightV2Weight,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "Execute";
            params: {
              message: XcmVersionedXcm;
              maxWeight: SpWeightsWeightV2Weight;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Extoll that a particular destination can be communicated with through a particular
     * version of XCM.
     *
     * - `origin`: Must be an origin specified by AdminOrigin.
     * - `location`: The destination that is being described.
     * - `xcm_version`: The latest version of XCM that `location` supports.
     *
     * @param {StagingXcmV5Location} location
     * @param {number} version
     **/
    forceXcmVersion: GenericTxCall<
      (
        location: StagingXcmV5Location,
        version: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "ForceXcmVersion";
            params: { location: StagingXcmV5Location; version: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Set a safe XCM version (the version that XCM should be encoded with if the most recent
     * version a destination can accept is unknown).
     *
     * - `origin`: Must be an origin specified by AdminOrigin.
     * - `maybe_xcm_version`: The default XCM encoding version, or `None` to disable.
     *
     * @param {number | undefined} maybeXcmVersion
     **/
    forceDefaultXcmVersion: GenericTxCall<
      (maybeXcmVersion: number | undefined) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "ForceDefaultXcmVersion";
            params: { maybeXcmVersion: number | undefined };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Ask a location to notify us regarding their XCM version and any changes to it.
     *
     * - `origin`: Must be an origin specified by AdminOrigin.
     * - `location`: The location to which we should subscribe for XCM version notifications.
     *
     * @param {XcmVersionedLocation} location
     **/
    forceSubscribeVersionNotify: GenericTxCall<
      (location: XcmVersionedLocation) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "ForceSubscribeVersionNotify";
            params: { location: XcmVersionedLocation };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Require that a particular destination should no longer notify us regarding any XCM
     * version changes.
     *
     * - `origin`: Must be an origin specified by AdminOrigin.
     * - `location`: The location to which we are currently subscribed for XCM version
     * notifications which we no longer desire.
     *
     * @param {XcmVersionedLocation} location
     **/
    forceUnsubscribeVersionNotify: GenericTxCall<
      (location: XcmVersionedLocation) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "ForceUnsubscribeVersionNotify";
            params: { location: XcmVersionedLocation };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Transfer some assets from the local chain to the destination chain through their local,
     * destination or remote reserve.
     *
     * `assets` must have same reserve location and may not be teleportable to `dest`.
     * - `assets` have local reserve: transfer assets to sovereign account of destination
     * chain and forward a notification XCM to `dest` to mint and deposit reserve-based
     * assets to `beneficiary`.
     * - `assets` have destination reserve: burn local assets and forward a notification to
     * `dest` chain to withdraw the reserve assets from this chain's sovereign account and
     * deposit them to `beneficiary`.
     * - `assets` have remote reserve: burn local assets, forward XCM to reserve chain to move
     * reserves from this chain's SA to `dest` chain's SA, and forward another XCM to `dest`
     * to mint and deposit reserve-based assets to `beneficiary`.
     *
     * Fee payment on the destination side is made from the asset in the `assets` vector of
     * index `fee_asset_item`, up to enough to pay for `weight_limit` of weight. If more weight
     * is needed than `weight_limit`, then the operation will fail and the sent assets may be
     * at risk.
     *
     * - `origin`: Must be capable of withdrawing the `assets` and executing XCM.
     * - `dest`: Destination context for the assets. Will typically be `[Parent,
     * Parachain(..)]` to send from parachain to parachain, or `[Parachain(..)]` to send from
     * relay to parachain.
     * - `beneficiary`: A beneficiary location for the assets in the context of `dest`. Will
     * generally be an `AccountId32` value.
     * - `assets`: The assets to be withdrawn. This should include the assets used to pay the
     * fee on the `dest` (and possibly reserve) chains.
     * - `fee_asset_item`: The index into `assets` of the item which should be used to pay
     * fees.
     * - `weight_limit`: The remote-side weight limit, if any, for the XCM fee purchase.
     *
     * @param {XcmVersionedLocation} dest
     * @param {XcmVersionedLocation} beneficiary
     * @param {XcmVersionedAssets} assets
     * @param {number} feeAssetItem
     * @param {XcmV3WeightLimit} weightLimit
     **/
    limitedReserveTransferAssets: GenericTxCall<
      (
        dest: XcmVersionedLocation,
        beneficiary: XcmVersionedLocation,
        assets: XcmVersionedAssets,
        feeAssetItem: number,
        weightLimit: XcmV3WeightLimit,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "LimitedReserveTransferAssets";
            params: {
              dest: XcmVersionedLocation;
              beneficiary: XcmVersionedLocation;
              assets: XcmVersionedAssets;
              feeAssetItem: number;
              weightLimit: XcmV3WeightLimit;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Teleport some assets from the local chain to some destination chain.
     *
     * Fee payment on the destination side is made from the asset in the `assets` vector of
     * index `fee_asset_item`, up to enough to pay for `weight_limit` of weight. If more weight
     * is needed than `weight_limit`, then the operation will fail and the sent assets may be
     * at risk.
     *
     * - `origin`: Must be capable of withdrawing the `assets` and executing XCM.
     * - `dest`: Destination context for the assets. Will typically be `[Parent,
     * Parachain(..)]` to send from parachain to parachain, or `[Parachain(..)]` to send from
     * relay to parachain.
     * - `beneficiary`: A beneficiary location for the assets in the context of `dest`. Will
     * generally be an `AccountId32` value.
     * - `assets`: The assets to be withdrawn. This should include the assets used to pay the
     * fee on the `dest` chain.
     * - `fee_asset_item`: The index into `assets` of the item which should be used to pay
     * fees.
     * - `weight_limit`: The remote-side weight limit, if any, for the XCM fee purchase.
     *
     * @param {XcmVersionedLocation} dest
     * @param {XcmVersionedLocation} beneficiary
     * @param {XcmVersionedAssets} assets
     * @param {number} feeAssetItem
     * @param {XcmV3WeightLimit} weightLimit
     **/
    limitedTeleportAssets: GenericTxCall<
      (
        dest: XcmVersionedLocation,
        beneficiary: XcmVersionedLocation,
        assets: XcmVersionedAssets,
        feeAssetItem: number,
        weightLimit: XcmV3WeightLimit,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "LimitedTeleportAssets";
            params: {
              dest: XcmVersionedLocation;
              beneficiary: XcmVersionedLocation;
              assets: XcmVersionedAssets;
              feeAssetItem: number;
              weightLimit: XcmV3WeightLimit;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Set or unset the global suspension state of the XCM executor.
     *
     * - `origin`: Must be an origin specified by AdminOrigin.
     * - `suspended`: `true` to suspend, `false` to resume.
     *
     * @param {boolean} suspended
     **/
    forceSuspension: GenericTxCall<
      (suspended: boolean) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "ForceSuspension";
            params: { suspended: boolean };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Transfer some assets from the local chain to the destination chain through their local,
     * destination or remote reserve, or through teleports.
     *
     * Fee payment on the destination side is made from the asset in the `assets` vector of
     * index `fee_asset_item` (hence referred to as `fees`), up to enough to pay for
     * `weight_limit` of weight. If more weight is needed than `weight_limit`, then the
     * operation will fail and the sent assets may be at risk.
     *
     * `assets` (excluding `fees`) must have same reserve location or otherwise be teleportable
     * to `dest`, no limitations imposed on `fees`.
     * - for local reserve: transfer assets to sovereign account of destination chain and
     * forward a notification XCM to `dest` to mint and deposit reserve-based assets to
     * `beneficiary`.
     * - for destination reserve: burn local assets and forward a notification to `dest` chain
     * to withdraw the reserve assets from this chain's sovereign account and deposit them
     * to `beneficiary`.
     * - for remote reserve: burn local assets, forward XCM to reserve chain to move reserves
     * from this chain's SA to `dest` chain's SA, and forward another XCM to `dest` to mint
     * and deposit reserve-based assets to `beneficiary`.
     * - for teleports: burn local assets and forward XCM to `dest` chain to mint/teleport
     * assets and deposit them to `beneficiary`.
     *
     * - `origin`: Must be capable of withdrawing the `assets` and executing XCM.
     * - `dest`: Destination context for the assets. Will typically be `X2(Parent,
     * Parachain(..))` to send from parachain to parachain, or `X1(Parachain(..))` to send
     * from relay to parachain.
     * - `beneficiary`: A beneficiary location for the assets in the context of `dest`. Will
     * generally be an `AccountId32` value.
     * - `assets`: The assets to be withdrawn. This should include the assets used to pay the
     * fee on the `dest` (and possibly reserve) chains.
     * - `fee_asset_item`: The index into `assets` of the item which should be used to pay
     * fees.
     * - `weight_limit`: The remote-side weight limit, if any, for the XCM fee purchase.
     *
     * @param {XcmVersionedLocation} dest
     * @param {XcmVersionedLocation} beneficiary
     * @param {XcmVersionedAssets} assets
     * @param {number} feeAssetItem
     * @param {XcmV3WeightLimit} weightLimit
     **/
    transferAssets: GenericTxCall<
      (
        dest: XcmVersionedLocation,
        beneficiary: XcmVersionedLocation,
        assets: XcmVersionedAssets,
        feeAssetItem: number,
        weightLimit: XcmV3WeightLimit,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "TransferAssets";
            params: {
              dest: XcmVersionedLocation;
              beneficiary: XcmVersionedLocation;
              assets: XcmVersionedAssets;
              feeAssetItem: number;
              weightLimit: XcmV3WeightLimit;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Claims assets trapped on this pallet because of leftover assets during XCM execution.
     *
     * - `origin`: Anyone can call this extrinsic.
     * - `assets`: The exact assets that were trapped. Use the version to specify what version
     * was the latest when they were trapped.
     * - `beneficiary`: The location/account where the claimed assets will be deposited.
     *
     * @param {XcmVersionedAssets} assets
     * @param {XcmVersionedLocation} beneficiary
     **/
    claimAssets: GenericTxCall<
      (
        assets: XcmVersionedAssets,
        beneficiary: XcmVersionedLocation,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "ClaimAssets";
            params: {
              assets: XcmVersionedAssets;
              beneficiary: XcmVersionedLocation;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Transfer assets from the local chain to the destination chain using explicit transfer
     * types for assets and fees.
     *
     * `assets` must have same reserve location or may be teleportable to `dest`. Caller must
     * provide the `assets_transfer_type` to be used for `assets`:
     * - `TransferType::LocalReserve`: transfer assets to sovereign account of destination
     * chain and forward a notification XCM to `dest` to mint and deposit reserve-based
     * assets to `beneficiary`.
     * - `TransferType::DestinationReserve`: burn local assets and forward a notification to
     * `dest` chain to withdraw the reserve assets from this chain's sovereign account and
     * deposit them to `beneficiary`.
     * - `TransferType::RemoteReserve(reserve)`: burn local assets, forward XCM to `reserve`
     * chain to move reserves from this chain's SA to `dest` chain's SA, and forward another
     * XCM to `dest` to mint and deposit reserve-based assets to `beneficiary`. Typically
     * the remote `reserve` is Asset Hub.
     * - `TransferType::Teleport`: burn local assets and forward XCM to `dest` chain to
     * mint/teleport assets and deposit them to `beneficiary`.
     *
     * On the destination chain, as well as any intermediary hops, `BuyExecution` is used to
     * buy execution using transferred `assets` identified by `remote_fees_id`.
     * Make sure enough of the specified `remote_fees_id` asset is included in the given list
     * of `assets`. `remote_fees_id` should be enough to pay for `weight_limit`. If more weight
     * is needed than `weight_limit`, then the operation will fail and the sent assets may be
     * at risk.
     *
     * `remote_fees_id` may use different transfer type than rest of `assets` and can be
     * specified through `fees_transfer_type`.
     *
     * The caller needs to specify what should happen to the transferred assets once they reach
     * the `dest` chain. This is done through the `custom_xcm_on_dest` parameter, which
     * contains the instructions to execute on `dest` as a final step.
     * This is usually as simple as:
     * `Xcm(vec![DepositAsset { assets: Wild(AllCounted(assets.len())), beneficiary }])`,
     * but could be something more exotic like sending the `assets` even further.
     *
     * - `origin`: Must be capable of withdrawing the `assets` and executing XCM.
     * - `dest`: Destination context for the assets. Will typically be `[Parent,
     * Parachain(..)]` to send from parachain to parachain, or `[Parachain(..)]` to send from
     * relay to parachain, or `(parents: 2, (GlobalConsensus(..), ..))` to send from
     * parachain across a bridge to another ecosystem destination.
     * - `assets`: The assets to be withdrawn. This should include the assets used to pay the
     * fee on the `dest` (and possibly reserve) chains.
     * - `assets_transfer_type`: The XCM `TransferType` used to transfer the `assets`.
     * - `remote_fees_id`: One of the included `assets` to be used to pay fees.
     * - `fees_transfer_type`: The XCM `TransferType` used to transfer the `fees` assets.
     * - `custom_xcm_on_dest`: The XCM to be executed on `dest` chain as the last step of the
     * transfer, which also determines what happens to the assets on the destination chain.
     * - `weight_limit`: The remote-side weight limit, if any, for the XCM fee purchase.
     *
     * @param {XcmVersionedLocation} dest
     * @param {XcmVersionedAssets} assets
     * @param {StagingXcmExecutorAssetTransferTransferType} assetsTransferType
     * @param {XcmVersionedAssetId} remoteFeesId
     * @param {StagingXcmExecutorAssetTransferTransferType} feesTransferType
     * @param {XcmVersionedXcm} customXcmOnDest
     * @param {XcmV3WeightLimit} weightLimit
     **/
    transferAssetsUsingTypeAndThen: GenericTxCall<
      (
        dest: XcmVersionedLocation,
        assets: XcmVersionedAssets,
        assetsTransferType: StagingXcmExecutorAssetTransferTransferType,
        remoteFeesId: XcmVersionedAssetId,
        feesTransferType: StagingXcmExecutorAssetTransferTransferType,
        customXcmOnDest: XcmVersionedXcm,
        weightLimit: XcmV3WeightLimit,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "TransferAssetsUsingTypeAndThen";
            params: {
              dest: XcmVersionedLocation;
              assets: XcmVersionedAssets;
              assetsTransferType: StagingXcmExecutorAssetTransferTransferType;
              remoteFeesId: XcmVersionedAssetId;
              feesTransferType: StagingXcmExecutorAssetTransferTransferType;
              customXcmOnDest: XcmVersionedXcm;
              weightLimit: XcmV3WeightLimit;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Authorize another `aliaser` location to alias into the local `origin` making this call.
     * The `aliaser` is only authorized until the provided `expiry` block number.
     * The call can also be used for a previously authorized alias in order to update its
     * `expiry` block number.
     *
     * Usually useful to allow your local account to be aliased into from a remote location
     * also under your control (like your account on another chain).
     *
     * WARNING: make sure the caller `origin` (you) trusts the `aliaser` location to act in
     * their/your name. Once authorized using this call, the `aliaser` can freely impersonate
     * `origin` in XCM programs executed on the local chain.
     *
     * @param {XcmVersionedLocation} aliaser
     * @param {bigint | undefined} expires
     **/
    addAuthorizedAlias: GenericTxCall<
      (
        aliaser: XcmVersionedLocation,
        expires: bigint | undefined,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "AddAuthorizedAlias";
            params: {
              aliaser: XcmVersionedLocation;
              expires: bigint | undefined;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Remove a previously authorized `aliaser` from the list of locations that can alias into
     * the local `origin` making this call.
     *
     * @param {XcmVersionedLocation} aliaser
     **/
    removeAuthorizedAlias: GenericTxCall<
      (aliaser: XcmVersionedLocation) => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "RemoveAuthorizedAlias";
            params: { aliaser: XcmVersionedLocation };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Remove all previously authorized `aliaser`s that can alias into the local `origin`
     * making this call.
     *
     **/
    removeAllAuthorizedAliases: GenericTxCall<
      () => ChainSubmittableExtrinsic<
        {
          pallet: "PolkadotXcm";
          palletCall: {
            name: "RemoveAllAuthorizedAliases";
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `CumulusXcm`'s transaction calls
   **/
  cumulusXcm: {
    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `MessageQueue`'s transaction calls
   **/
  messageQueue: {
    /**
     * Remove a page which has no more messages remaining to be processed or is stale.
     *
     * @param {CumulusPrimitivesCoreAggregateMessageOrigin} messageOrigin
     * @param {number} pageIndex
     **/
    reapPage: GenericTxCall<
      (
        messageOrigin: CumulusPrimitivesCoreAggregateMessageOrigin,
        pageIndex: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "MessageQueue";
          palletCall: {
            name: "ReapPage";
            params: {
              messageOrigin: CumulusPrimitivesCoreAggregateMessageOrigin;
              pageIndex: number;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Execute an overweight message.
     *
     * Temporary processing errors will be propagated whereas permanent errors are treated
     * as success condition.
     *
     * - `origin`: Must be `Signed`.
     * - `message_origin`: The origin from which the message to be executed arrived.
     * - `page`: The page in the queue in which the message to be executed is sitting.
     * - `index`: The index into the queue of the message to be executed.
     * - `weight_limit`: The maximum amount of weight allowed to be consumed in the execution
     * of the message.
     *
     * Benchmark complexity considerations: O(index + weight_limit).
     *
     * @param {CumulusPrimitivesCoreAggregateMessageOrigin} messageOrigin
     * @param {number} page
     * @param {number} index
     * @param {SpWeightsWeightV2Weight} weightLimit
     **/
    executeOverweight: GenericTxCall<
      (
        messageOrigin: CumulusPrimitivesCoreAggregateMessageOrigin,
        page: number,
        index: number,
        weightLimit: SpWeightsWeightV2Weight,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "MessageQueue";
          palletCall: {
            name: "ExecuteOverweight";
            params: {
              messageOrigin: CumulusPrimitivesCoreAggregateMessageOrigin;
              page: number;
              index: number;
              weightLimit: SpWeightsWeightV2Weight;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `TemplatePallet`'s transaction calls
   **/
  templatePallet: {
    /**
     * An example dispatchable that takes a singles value as a parameter, writes the value to
     * storage and emits an event. This function must be dispatched by a signed extrinsic.
     *
     * @param {number} bn
     **/
    doSomething: GenericTxCall<
      (bn: number) => ChainSubmittableExtrinsic<
        {
          pallet: "TemplatePallet";
          palletCall: {
            name: "DoSomething";
            params: { bn: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * An example dispatchable that may throw a custom error.
     *
     **/
    causeError: GenericTxCall<
      () => ChainSubmittableExtrinsic<
        {
          pallet: "TemplatePallet";
          palletCall: {
            name: "CauseError";
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Keyword`'s transaction calls
   **/
  keyword: {
    /**
     *
     * @param {FixedBytes<32>} code
     * @param {BytesLike} name
     * @param {BytesLike} description
     * @param {TcPrimitivesKeywordKind} kind
     **/
    registerKeyword: GenericTxCall<
      (
        code: FixedBytes<32>,
        name: BytesLike,
        description: BytesLike,
        kind: TcPrimitivesKeywordKind,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Keyword";
          palletCall: {
            name: "RegisterKeyword";
            params: {
              code: FixedBytes<32>;
              name: BytesLike;
              description: BytesLike;
              kind: TcPrimitivesKeywordKind;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {FixedBytes<32>} code
     **/
    removeKeyword: GenericTxCall<
      (code: FixedBytes<32>) => ChainSubmittableExtrinsic<
        {
          pallet: "Keyword";
          palletCall: {
            name: "RemoveKeyword";
            params: { code: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Skill`'s transaction calls
   **/
  skill: {
    /**
     *
     * @param {FixedBytes<32>} code
     * @param {BytesLike} name
     * @param {BytesLike} description
     **/
    registerSkill: GenericTxCall<
      (
        code: FixedBytes<32>,
        name: BytesLike,
        description: BytesLike,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Skill";
          palletCall: {
            name: "RegisterSkill";
            params: {
              code: FixedBytes<32>;
              name: BytesLike;
              description: BytesLike;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {FixedBytes<32>} code
     **/
    removeSkill: GenericTxCall<
      (code: FixedBytes<32>) => ChainSubmittableExtrinsic<
        {
          pallet: "Skill";
          palletCall: {
            name: "RemoveSkill";
            params: { code: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Faction`'s transaction calls
   **/
  faction: {
    /**
     *
     * @param {FixedBytes<16>} code
     * @param {BytesLike} name
     * @param {TcPrimitivesAlignment} alignment
     * @param {Array<FixedBytes<32>>} keywords
     **/
    registerFaction: GenericTxCall<
      (
        code: FixedBytes<16>,
        name: BytesLike,
        alignment: TcPrimitivesAlignment,
        keywords: Array<FixedBytes<32>>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Faction";
          palletCall: {
            name: "RegisterFaction";
            params: {
              code: FixedBytes<16>;
              name: BytesLike;
              alignment: TcPrimitivesAlignment;
              keywords: Array<FixedBytes<32>>;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {FixedBytes<16>} variant
     * @param {FixedBytes<16>} parent
     * @param {BytesLike} name
     * @param {TcPrimitivesAlignment} alignment
     * @param {Array<FixedBytes<32>>} keywords
     **/
    registerVariant: GenericTxCall<
      (
        variant: FixedBytes<16>,
        parent: FixedBytes<16>,
        name: BytesLike,
        alignment: TcPrimitivesAlignment,
        keywords: Array<FixedBytes<32>>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Faction";
          palletCall: {
            name: "RegisterVariant";
            params: {
              variant: FixedBytes<16>;
              parent: FixedBytes<16>;
              name: BytesLike;
              alignment: TcPrimitivesAlignment;
              keywords: Array<FixedBytes<32>>;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {FixedBytes<16>} code
     * @param {Array<FixedBytes<32>>} keywords
     **/
    updateKeywords: GenericTxCall<
      (
        code: FixedBytes<16>,
        keywords: Array<FixedBytes<32>>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Faction";
          palletCall: {
            name: "UpdateKeywords";
            params: { code: FixedBytes<16>; keywords: Array<FixedBytes<32>> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Battlekit`'s transaction calls
   **/
  battlekit: {
    /**
     *
     * @param {FixedBytes<32>} code
     * @param {BytesLike} name
     * @param {BytesLike} description
     * @param {TcPrimitivesBattlekitType} battlekitType
     * @param {TcPrimitivesWeaponRange} range
     * @param {number} cost
     * @param {Array<FixedBytes<32>>} keywords
     **/
    registerItem: GenericTxCall<
      (
        code: FixedBytes<32>,
        name: BytesLike,
        description: BytesLike,
        battlekitType: TcPrimitivesBattlekitType,
        range: TcPrimitivesWeaponRange,
        cost: number,
        keywords: Array<FixedBytes<32>>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Battlekit";
          palletCall: {
            name: "RegisterItem";
            params: {
              code: FixedBytes<32>;
              name: BytesLike;
              description: BytesLike;
              battlekitType: TcPrimitivesBattlekitType;
              range: TcPrimitivesWeaponRange;
              cost: number;
              keywords: Array<FixedBytes<32>>;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {FixedBytes<32>} code
     **/
    removeItem: GenericTxCall<
      (code: FixedBytes<32>) => ChainSubmittableExtrinsic<
        {
          pallet: "Battlekit";
          palletCall: {
            name: "RemoveItem";
            params: { code: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Armoury`'s transaction calls
   **/
  armoury: {
    /**
     *
     * @param {FixedBytes<16>} faction
     * @param {FixedBytes<32>} item
     **/
    addEntry: GenericTxCall<
      (
        faction: FixedBytes<16>,
        item: FixedBytes<32>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Armoury";
          palletCall: {
            name: "AddEntry";
            params: { faction: FixedBytes<16>; item: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {FixedBytes<16>} faction
     * @param {FixedBytes<32>} item
     **/
    removeEntry: GenericTxCall<
      (
        faction: FixedBytes<16>,
        item: FixedBytes<32>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Armoury";
          palletCall: {
            name: "RemoveEntry";
            params: { faction: FixedBytes<16>; item: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Entry`'s transaction calls
   **/
  entry: {
    /**
     *
     * @param {FixedBytes<32>} code
     * @param {BytesLike} name
     * @param {FixedBytes<16>} faction
     * @param {number} minCount
     * @param {number | undefined} maxCount
     * @param {number} cost
     * @param {TcPrimitivesStatProfile} profile
     * @param {BytesLike} description
     * @param {Array<FixedBytes<32>>} keywords
     **/
    registerEntry: GenericTxCall<
      (
        code: FixedBytes<32>,
        name: BytesLike,
        faction: FixedBytes<16>,
        minCount: number,
        maxCount: number | undefined,
        cost: number,
        profile: TcPrimitivesStatProfile,
        description: BytesLike,
        keywords: Array<FixedBytes<32>>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Entry";
          palletCall: {
            name: "RegisterEntry";
            params: {
              code: FixedBytes<32>;
              name: BytesLike;
              faction: FixedBytes<16>;
              minCount: number;
              maxCount: number | undefined;
              cost: number;
              profile: TcPrimitivesStatProfile;
              description: BytesLike;
              keywords: Array<FixedBytes<32>>;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {FixedBytes<32>} code
     **/
    removeEntry: GenericTxCall<
      (code: FixedBytes<32>) => ChainSubmittableExtrinsic<
        {
          pallet: "Entry";
          palletCall: {
            name: "RemoveEntry";
            params: { code: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Patron`'s transaction calls
   **/
  patron: {
    /**
     *
     * @param {FixedBytes<32>} code
     * @param {BytesLike} name
     * @param {BytesLike} description
     * @param {Array<FixedBytes<16>>} factions
     * @param {Array<FixedBytes<32>>} skills
     **/
    registerPatron: GenericTxCall<
      (
        code: FixedBytes<32>,
        name: BytesLike,
        description: BytesLike,
        factions: Array<FixedBytes<16>>,
        skills: Array<FixedBytes<32>>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Patron";
          palletCall: {
            name: "RegisterPatron";
            params: {
              code: FixedBytes<32>;
              name: BytesLike;
              description: BytesLike;
              factions: Array<FixedBytes<16>>;
              skills: Array<FixedBytes<32>>;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {FixedBytes<32>} code
     **/
    removePatron: GenericTxCall<
      (code: FixedBytes<32>) => ChainSubmittableExtrinsic<
        {
          pallet: "Patron";
          palletCall: {
            name: "RemovePatron";
            params: { code: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Warband`'s transaction calls
   **/
  warband: {
    /**
     *
     * @param {FixedBytes<16>} faction
     * @param {FixedBytes<32>} patron
     * @param {BytesLike} name
     **/
    createWarband: GenericTxCall<
      (
        faction: FixedBytes<16>,
        patron: FixedBytes<32>,
        name: BytesLike,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Warband";
          palletCall: {
            name: "CreateWarband";
            params: {
              faction: FixedBytes<16>;
              patron: FixedBytes<32>;
              name: BytesLike;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     **/
    disbandWarband: GenericTxCall<
      (warbandId: number) => ChainSubmittableExtrinsic<
        {
          pallet: "Warband";
          palletCall: {
            name: "DisbandWarband";
            params: { warbandId: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     * @param {number} campaignId
     **/
    enrollInCampaign: GenericTxCall<
      (
        warbandId: number,
        campaignId: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Warband";
          palletCall: {
            name: "EnrollInCampaign";
            params: { warbandId: number; campaignId: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     **/
    withdrawFromCampaign: GenericTxCall<
      (warbandId: number) => ChainSubmittableExtrinsic<
        {
          pallet: "Warband";
          palletCall: {
            name: "WithdrawFromCampaign";
            params: { warbandId: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Roster`'s transaction calls
   **/
  roster: {
    /**
     *
     * @param {number} warbandId
     * @param {FixedBytes<32>} entryCode
     * @param {BytesLike} name
     * @param {Array<FixedBytes<32>>} items
     **/
    recruit: GenericTxCall<
      (
        warbandId: number,
        entryCode: FixedBytes<32>,
        name: BytesLike,
        items: Array<FixedBytes<32>>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Roster";
          palletCall: {
            name: "Recruit";
            params: {
              warbandId: number;
              entryCode: FixedBytes<32>;
              name: BytesLike;
              items: Array<FixedBytes<32>>;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     * @param {number} slot
     **/
    dismiss: GenericTxCall<
      (
        warbandId: number,
        slot: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Roster";
          palletCall: {
            name: "Dismiss";
            params: { warbandId: number; slot: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     * @param {number} slot
     * @param {FixedBytes<32>} item
     **/
    equipItem: GenericTxCall<
      (
        warbandId: number,
        slot: number,
        item: FixedBytes<32>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Roster";
          palletCall: {
            name: "EquipItem";
            params: { warbandId: number; slot: number; item: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     * @param {number} slot
     * @param {number} itemIndex
     **/
    unequipItem: GenericTxCall<
      (
        warbandId: number,
        slot: number,
        itemIndex: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Roster";
          palletCall: {
            name: "UnequipItem";
            params: { warbandId: number; slot: number; itemIndex: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     * @param {number} slot
     * @param {number} itemIndex
     **/
    sellItem: GenericTxCall<
      (
        warbandId: number,
        slot: number,
        itemIndex: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Roster";
          palletCall: {
            name: "SellItem";
            params: { warbandId: number; slot: number; itemIndex: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     * @param {number} slot
     * @param {number} amount
     **/
    addXp: GenericTxCall<
      (
        warbandId: number,
        slot: number,
        amount: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Roster";
          palletCall: {
            name: "AddXp";
            params: { warbandId: number; slot: number; amount: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     * @param {number} slot
     * @param {number} scarsToAdd
     **/
    applyTrauma: GenericTxCall<
      (
        warbandId: number,
        slot: number,
        scarsToAdd: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Roster";
          palletCall: {
            name: "ApplyTrauma";
            params: { warbandId: number; slot: number; scarsToAdd: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     * @param {number} slot
     **/
    promoteToElite: GenericTxCall<
      (
        warbandId: number,
        slot: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Roster";
          palletCall: {
            name: "PromoteToElite";
            params: { warbandId: number; slot: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     * @param {number} slot
     * @param {FixedBytes<32>} skill
     **/
    learnSkill: GenericTxCall<
      (
        warbandId: number,
        slot: number,
        skill: FixedBytes<32>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Roster";
          palletCall: {
            name: "LearnSkill";
            params: { warbandId: number; slot: number; skill: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Campaign`'s transaction calls
   **/
  campaign: {
    /**
     *
     * @param {BytesLike} name
     * @param {BytesLike} theatreId
     * @param {number} maxWarbands
     **/
    createCampaign: GenericTxCall<
      (
        name: BytesLike,
        theatreId: BytesLike,
        maxWarbands: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Campaign";
          palletCall: {
            name: "CreateCampaign";
            params: {
              name: BytesLike;
              theatreId: BytesLike;
              maxWarbands: number;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} campaignId
     **/
    startCampaign: GenericTxCall<
      (campaignId: number) => ChainSubmittableExtrinsic<
        {
          pallet: "Campaign";
          palletCall: {
            name: "StartCampaign";
            params: { campaignId: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} campaignId
     **/
    advanceGame: GenericTxCall<
      (campaignId: number) => ChainSubmittableExtrinsic<
        {
          pallet: "Campaign";
          palletCall: {
            name: "AdvanceGame";
            params: { campaignId: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} campaignId
     **/
    concludeCampaign: GenericTxCall<
      (campaignId: number) => ChainSubmittableExtrinsic<
        {
          pallet: "Campaign";
          palletCall: {
            name: "ConcludeCampaign";
            params: { campaignId: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} campaignId
     * @param {number} warbandId
     **/
    enrollWarband: GenericTxCall<
      (
        campaignId: number,
        warbandId: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Campaign";
          palletCall: {
            name: "EnrollWarband";
            params: { campaignId: number; warbandId: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} campaignId
     * @param {number} warbandId
     **/
    withdrawWarband: GenericTxCall<
      (
        campaignId: number,
        warbandId: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Campaign";
          palletCall: {
            name: "WithdrawWarband";
            params: { campaignId: number; warbandId: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Battle`'s transaction calls
   **/
  battle: {
    /**
     *
     * @param {number} campaignId
     * @param {FixedBytes<32>} territoryId
     * @param {number} warbandId
     **/
    challenge: GenericTxCall<
      (
        campaignId: number,
        territoryId: FixedBytes<32>,
        warbandId: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Battle";
          palletCall: {
            name: "Challenge";
            params: {
              campaignId: number;
              territoryId: FixedBytes<32>;
              warbandId: number;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} battleId
     * @param {number} warbandId
     **/
    acceptChallenge: GenericTxCall<
      (
        battleId: number,
        warbandId: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Battle";
          palletCall: {
            name: "AcceptChallenge";
            params: { battleId: number; warbandId: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} battleId
     * @param {number} warbandId
     * @param {PalletBattleBattleReport} report
     **/
    submitReport: GenericTxCall<
      (
        battleId: number,
        warbandId: number,
        report: PalletBattleBattleReport,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Battle";
          palletCall: {
            name: "SubmitReport";
            params: {
              battleId: number;
              warbandId: number;
              report: PalletBattleBattleReport;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} battleId
     **/
    computeResult: GenericTxCall<
      (battleId: number) => ChainSubmittableExtrinsic<
        {
          pallet: "Battle";
          palletCall: {
            name: "ComputeResult";
            params: { battleId: number };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Territory`'s transaction calls
   **/
  territory: {
    /**
     *
     * @param {number} campaignId
     * @param {FixedBytes<32>} territory
     **/
    initializeTerritory: GenericTxCall<
      (
        campaignId: number,
        territory: FixedBytes<32>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Territory";
          palletCall: {
            name: "InitializeTerritory";
            params: { campaignId: number; territory: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} campaignId
     * @param {FixedBytes<32>} territory
     * @param {number} warbandId
     **/
    capture: GenericTxCall<
      (
        campaignId: number,
        territory: FixedBytes<32>,
        warbandId: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Territory";
          palletCall: {
            name: "Capture";
            params: {
              campaignId: number;
              territory: FixedBytes<32>;
              warbandId: number;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} campaignId
     * @param {FixedBytes<32>} territory
     * @param {FixedBytes<32>} buildingCode
     * @param {number} warbandId
     **/
    constructBuilding: GenericTxCall<
      (
        campaignId: number,
        territory: FixedBytes<32>,
        buildingCode: FixedBytes<32>,
        warbandId: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Territory";
          palletCall: {
            name: "ConstructBuilding";
            params: {
              campaignId: number;
              territory: FixedBytes<32>;
              buildingCode: FixedBytes<32>;
              warbandId: number;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} campaignId
     * @param {FixedBytes<32>} territory
     * @param {number} buildingIndex
     **/
    demolishBuilding: GenericTxCall<
      (
        campaignId: number,
        territory: FixedBytes<32>,
        buildingIndex: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Territory";
          palletCall: {
            name: "DemolishBuilding";
            params: {
              campaignId: number;
              territory: FixedBytes<32>;
              buildingIndex: number;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} campaignId
     * @param {FixedBytes<32>} territory
     * @param {number} level
     **/
    updateSupply: GenericTxCall<
      (
        campaignId: number,
        territory: FixedBytes<32>,
        level: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Territory";
          palletCall: {
            name: "UpdateSupply";
            params: {
              campaignId: number;
              territory: FixedBytes<32>;
              level: number;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Building`'s transaction calls
   **/
  building: {
    /**
     *
     * @param {FixedBytes<32>} code
     * @param {BytesLike} name
     * @param {number} production
     * @param {number} costDucats
     * @param {Array<FixedBytes<16>>} allowedTerrains
     **/
    registerBuilding: GenericTxCall<
      (
        code: FixedBytes<32>,
        name: BytesLike,
        production: number,
        costDucats: number,
        allowedTerrains: Array<FixedBytes<16>>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Building";
          palletCall: {
            name: "RegisterBuilding";
            params: {
              code: FixedBytes<32>;
              name: BytesLike;
              production: number;
              costDucats: number;
              allowedTerrains: Array<FixedBytes<16>>;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {FixedBytes<32>} code
     **/
    removeBuilding: GenericTxCall<
      (code: FixedBytes<32>) => ChainSubmittableExtrinsic<
        {
          pallet: "Building";
          palletCall: {
            name: "RemoveBuilding";
            params: { code: FixedBytes<32> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
  /**
   * Pallet `Exploration`'s transaction calls
   **/
  exploration: {
    /**
     * Perform exploration roll using on-chain randomness.
     * `seed` is a user-provided entropy mixed with block hash.
     *
     * @param {number} warbandId
     * @param {number} battleId
     * @param {TcPrimitivesExplorationTable} tableChoice
     * @param {bigint} seed
     **/
    explore: GenericTxCall<
      (
        warbandId: number,
        battleId: number,
        tableChoice: TcPrimitivesExplorationTable,
        seed: bigint,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Exploration";
          palletCall: {
            name: "Explore";
            params: {
              warbandId: number;
              battleId: number;
              tableChoice: TcPrimitivesExplorationTable;
              seed: bigint;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     *
     * @param {number} warbandId
     * @param {number} battleId
     * @param {number} optionIndex
     **/
    chooseDiscoveryOption: GenericTxCall<
      (
        warbandId: number,
        battleId: number,
        optionIndex: number,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Exploration";
          palletCall: {
            name: "ChooseDiscoveryOption";
            params: {
              warbandId: number;
              battleId: number;
              optionIndex: number;
            };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Grant an exploration skill to a warband (called by governance/root after discovery).
     *
     * @param {number} warbandId
     * @param {FixedBytes<16>} skill
     **/
    grantExplorationSkill: GenericTxCall<
      (
        warbandId: number,
        skill: FixedBytes<16>,
      ) => ChainSubmittableExtrinsic<
        {
          pallet: "Exploration";
          palletCall: {
            name: "GrantExplorationSkill";
            params: { warbandId: number; skill: FixedBytes<16> };
          };
        },
        ChainKnownTypes
      >
    >;

    /**
     * Generic pallet tx call
     **/
    [callName: string]: GenericTxCall<TxCall<ChainKnownTypes>>;
  };
}
